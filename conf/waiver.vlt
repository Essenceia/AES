`verilator_config 

lint_off -rule TIMESCALEMOD -file "*" 
lint_off -rule DECLFILENAME -file "*/utils.v"
lint_off -rule UNUSEDSIGNAL -file "*" -match "Signal is not used: 'debug_*"
lint_off -rule UNUSEDSIGNAL -file "*" -match "Signal is not used: '*_unused"
